#include<stdio.h>
int main()
{
	//after freeing a structure free(s), if we print it shows garbage values. Those garbage values are dangling pointer error.
	//so we use null pointer after freeing to print no values but shows (null)
}